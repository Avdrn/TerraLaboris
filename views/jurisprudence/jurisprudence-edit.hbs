
<div class="signin">
  <form class="wrapper" action="/jurisprudence-editS" method="POST" id="form">
    <p> Hello {{user.username}}, wanna edit the jurisprudence to the database?</p>
    <h2>Modify the content of a decision</h2>
    <input type="text" placeholder="{{jurisprudence.author}}" name="author">
    <input type="text" placeholder="{{jurisprudence.content}}" name="content">
    <input type="date" placeholder="{{jurisprudence.date}}" name="date">
    <br>
    <input type="submit">
  </form>

</div>
